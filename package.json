{
  "name": "yuwen",
  "version": "0.1.0",
  "private": true,
  "description": "A clean Chinese handout editor for early learners.",
  "scripts": {
    "check": "node --check app.js && node --check vendor/data-chars-local.js"
  }
}
